export async function importDVI(_args: { shopId: number; vin: string; customerExternalId: string }) {
  // TODO: Hit AutoFlow DVI endpoint(s) and store in db.dvi (or db.inspections)
  return { ok: true };
}
